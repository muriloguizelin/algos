#include <bits/stdc++.h>

typedef long long ll;

using namespace std;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    vector<int> array;
    for (int i = 0; i < 3; i++) cin >> array[i];
    for (auto x : array) cout << x << "\n";
    
}
